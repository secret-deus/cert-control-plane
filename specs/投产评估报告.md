# Cert Control Plane 投产综合评估报告

- **评估日期**：2026-05-09
- **评估范围**：server/（后端 + 前端 + 部署）、client/（Python/Go/Rust Agent）、CI/CD、文档
- **评估方法**：代码审查 + 自动化测试验证 + 自测报告交叉验证
- **最后更新**：2026-05-11

---

## 总体结论

**有条件可投产，P0 投产阻塞项已处理，仍建议完成若干 P1 收尾验证。**

核心链路（证书上传 → 分配 → Agent 拉取 → nginx 部署 → 回滚）在真实 Docker/minikube 环境下已验证通过。CI 管道完整（含安全扫描），文档充分。此前的 DEV_MODE 生产护栏、基础限流、metrics/readiness、生产 compose 加固已处理；Go Agent PKCS8 解析、注册重试、部署回滚和真实 smoke 验证已补齐。

当前可进入预生产验证。Rust Agent 仍建议保持实验性/非生产发布范围，除非后续补齐回滚与 report-certs。

---

## 评估维度总览

| 维度 | 评级 | 一句话说明 |
|------|------|-----------|
| 代码架构 | **PASS** | 模块划分清晰，错误处理规范，无裸 except |
| 安全 | **CONCERN** | DEV_MODE 生产护栏与基础限流已处理，仍有 token 存储、CSP、fingerprint 校验等加固项 |
| 数据库 | **CONCERN** | 缺索引、serial_hex 唯一约束可能冲突 |
| 前端 | **CONCERN** | 无 CSP/安全头、无会话超时、CORS 过于宽松 |
| 部署 | **CONCERN** | 生产 compose 已加固，仍需完整镜像发布 smoke |
| 可观测性 | **CONCERN** | 已暴露基础 `/metrics`，仍需业务指标和审计归档 |
| 测试 | **CONCERN** | 150 后端测试、前端 lint/build/E2E 通过，但仍缺覆盖率门禁 |
| CI/CD | **PASS** | 3 条 workflow（CI 5 job + release + 安全扫描），优秀 |
| 文档 | **PASS** | README 完整、13 篇运维文档、OpenAPI 自动生成 |
| Python Agent | **PASS** | 最完整实现，有原子部署 + 回滚 |
| Go Agent | **PASS** | PKCS8、注册重试、部署回滚、真实 live smoke 已验证 |
| Rust Agent | **CONCERN** | RSA 2048（应 3072）、无回滚、无 report-certs |

---

## BLOCKER 阻塞项状态

### B1. DEV_MODE 无生产护栏 — 已修复

**位置**：`server/app/api/agent.py:346-354`, `server/app/api/agent.py:388-396`

**问题**：当 `DEV_MODE=true` 时，heartbeat 和 fetch-certs 跳过所有 Agent 认证，任何未认证请求都能拉到任意 Agent 的私钥。虽然 `config.py` 默认值为 `False`，但没有任何机制阻止在生产 `.env` 中误开。

**风险**：一旦误配置，所有证书私钥对外暴露。

**修复状态**：`Settings` 已拒绝 PostgreSQL 等非 SQLite 数据库启用 `DEV_MODE=true`，并补充配置护栏测试。

### B2. docker-compose.yml 是开发配置 — 已修复基础项

**位置**：`docker-compose.yml`

**问题**：当前文件存在多个生产不可用的配置：

| 问题点 | 行为 | 生产风险 |
|--------|------|----------|
| `--reload` 标志 | 文件变更自动重启 | 性能损耗，意外重启 |
| 源码 bind-mount（`:ro`） | 覆盖镜像内构建产物 | 容器内运行的不是镜像代码 |
| 无 `restart: unless-stopped` | 进程崩溃后不自动恢复 | 服务中断 |
| app 服务无 health check | 编排器不知道 app 是否就绪 | 流量打到未就绪容器 |
| 无资源限制 | 无 mem_limit / cpus 限制 | 单容器耗尽宿主资源 |
| 默认 PostgreSQL 密码 `postgres` | 弱密码 | 数据库暴露风险 |

**修复状态**：生产 compose 已设置必填 `POSTGRES_PASSWORD`、`restart: unless-stopped`、`/readyz` healthcheck、资源限制；开发 CORS 覆盖迁移到 `docker-compose.override.yml`。

### B3. 无限流（Rate Limiting）— 已修复基础项

**位置**：全局缺失

**问题**：所有端点均无限流保护。

| 端点 | 认证状态 | 暴露风险 |
|------|----------|----------|
| `POST /api/agent/register` | 无认证 | 洪泛创建百万条 pending 记录 |
| `GET /api/agent/register/status` | 无认证 | 枚举 Agent ID 和 fingerprint |
| `POST /api/control/*` | API Key 认证 | Admin API Key 暴力破解 |

**修复状态**：已加入内存窗口限流，覆盖 Agent 注册、注册状态轮询和 Admin API 认证失败。多实例生产环境仍建议在网关/Ingress 层配置分布式限流。

### B4. 无指标/监控 — 已修复基础项

**位置**：全局缺失

**问题**：没有 Prometheus、OpenTelemetry 或任何 metrics 集成。对于证书管理系统，缺少以下关键指标会导致运维盲区：

- 请求延迟 / 错误率
- 证书到期倒计时 gauge
- Agent 在线率
- Rollout 进度
- 数据库连接池使用率

**修复状态**：已暴露 `/metrics`，包含 `certcp_up`、`certcp_db_up`、`certcp_uptime_seconds`。业务级 Prometheus 指标仍作为后续增强项。

### B5. Go Agent PKCS8 解析 bug + 零测试 — 已修复

**位置**：`client/agent-go/internal/client/client.go`, `client/agent-go/internal/runner/runner.go`

**问题**：

1. `keyAny.(*interface{})` 类型断言几乎必然 panic。PKCS8 解析后应使用 `*rsa.PrivateKey` 等具体类型。
2. 整个 Go Agent 代码库没有任何 `_test.go` 文件，此 bug 无法被 CI 拦截。
3. 回滚逻辑不完整：chain 文件写入失败时不回滚已写入的 cert/key。
4. `copyFile` 始终设置 0644 权限，私钥备份文件权限过大。

**修复状态**：

- PKCS8 RSA 私钥按 `*rsa.PrivateKey` 解析并计算公钥 fingerprint，非 RSA 私钥返回明确错误。
- Go Agent 已补 `_test.go`，覆盖 PKCS8 fingerprint、非 RSA/非法 key、`.crt`/`.pem` 同级 key/chain 路径、reload 失败回滚、注册临时失败重试和 4xx 不重试。
- 部署回滚已覆盖 cert/key/chain，并恢复原文件权限；新增目标在失败时会清理已写入文件。
- 真实 Go Agent smoke 已通过：`COMPOSE_PROJECT_NAME=certcp_go_smoke SMOKE_EDGE_OVERLAY=tools/agent-smoke/docker-compose.edge-go.yml SMOKE_PUBLIC_PORT=9445 bash tools/agent-smoke/run-live-smoke.sh`，结果 serial `37A568E00001EFA53240104083A864F25CD29706` 成功部署到 `https://localhost:9445`。

---

## CONCERN 关注项（建议投产前或投产后尽快修复）

### 安全类

| 编号 | 问题 | 位置 | 建议 |
|------|------|------|------|
| C1 | fingerprint 未校验格式 | `server/app/schemas.py`, `server/app/api/agent.py` | **已修复**：注册 body 与 status query 均限制 lowercase 64 位 SHA-256 hex |
| C2 | Agent Token 明文存库 | `server/app/models.py:70` | 存 SHA-256 hash，查询时比对 hash |
| C3 | 无 CSP/安全响应头 | `server/app/main.py` | **已修复**：统一设置 CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy、HSTS |
| C4 | CORS 过于宽松 | `server/app/main.py` | **已修复**：限制为 `GET/POST/PUT/DELETE/OPTIONS` 和实际认证/内容头 |
| C5 | 前端无会话超时 | `server/frontend/src/App.tsx` | **已修复**：sessionStorage API Key 增加 30 分钟空闲超时 |
| C6 | shell=True 命令注入风险 | `client/agent/runner.py` | **已修复**：`shlex.split()` + `shell=False` |
| C7 | fingerprint 比较非时间安全 | `server/app/api/agent.py` | **已修复**：改用 `secrets.compare_digest` |

### 性能类

| 编号 | 问题 | 位置 | 建议 |
|------|------|------|------|
| C8 | list_agents N+1 查询 | `server/app/api/control.py:149-175` | 改为子查询聚合，避免 500 Agent 时 1001 次查询 |
| C9 | 缺少复合索引 | `server/app/models.py` | 在 `agent_cert_assignments` 上加 `(agent_id, local_path)` 索引 |
| C10 | `certificates.serial_hex` 唯一约束 | `server/app/models.py:162` | 多 Agent 部署同一证书会冲突，应改为联合唯一或去掉唯一 |

### 客户端 Agent 类

| 编号 | 问题 | Agent | 建议 |
|------|------|-------|------|
| C11 | RSA 密钥长度不一致 | Rust Agent | `client/agent-rust/src/crypto/mod.rs:18` 改为 3072 位 |
| C12 | 无部署回滚 | Rust Agent | `client/agent-rust/src/runner/mod.rs:204` 补充 backup-write-restore 逻辑 |
| C13 | 三 Agent 功能对齐差异大 | 全局 | 统一 report-certs、rollback、key size（见下方对齐矩阵） |
| C14 | Python Agent 客户端模块无测试 | Python Agent | 补充 `client.py`、`crypto.py`、`config.py` 测试 |

### 运维类

| 编号 | 问题 | 建议 |
|------|------|------|
| C15 | 无测试覆盖率度量 | 加 `pytest-cov` 到 dev 依赖，CI 设 `--cov=app --cov-fail-under=70` |
| C16 | 审计日志无归档/轮转 | 加定时清理或 PostgreSQL 表分区 |
| C18 | 无依赖锁文件 | 用 `pip-compile` 或 `uv lock` 锁定版本 |
| C19 | nginx.conf 引用了不存在的 Next.js 上游 | 更新 `server/nginx/nginx.conf` 或移除 stale 配置 |
| C20 | 代码重复 | liveness 计算和 urgency 分类在 3 处重复，提取为共享函数 |
| C21 | Dockerfile 使用 editable install | `server/Dockerfile:25` 改 `pip install .`（非 `-e .`） |

---

## PASS 项（已满足生产要求）

### 安全

- Fernet 加密私钥存储，启动时强制校验 `CA_KEY_ENCRYPTION_KEY` 非空
- `secrets.compare_digest` 时间安全比较 Admin API Key
- 全局 `Depends(verify_admin_key)` 保护 Control API 和 Dashboard API
- PostgreSQL 等非 SQLite 数据库禁止启用 `DEV_MODE=true`
- Agent 注册、注册状态轮询、Admin API 认证失败已有基础 IP 级限流
- SQLAlchemy 参数化查询，零 SQL 注入风险
- 前端无 `dangerouslySetInnerHTML` / `innerHTML` / `eval`，零 XSS 风险
- API Key 使用 sessionStorage（关闭标签页即清除），不使用 localStorage
- 前端 API 请求通过相对路径 `/api/control`，无硬编码地址

### 架构与代码

- 模块划分清晰：`core/`、`api/`、`orchestrator/`、`registry/` 职责明确
- Pydantic v2 Schema 对用户输入字段有 `min_length` / `max_length` 约束
- 错误处理规范：无裸 `except:`，所有 `except Exception` 均有合理用途
- Rollout 编排器异常隔离：per-rollout catch + 独立 session 标记失败
- `PaginatedResponse` 泛型分页模式统一复用

### 测试与 CI

- 后端 150 个测试全部通过，含真实 SQLite 集成测试与 Kubernetes Secret 凭据更新 validate 覆盖
- 前端 `npm run lint`、`npm run build`、`npm run test:e2e` 全部通过
- 客户端 15 个测试全部通过，含 cert 部署 + rollback 路径测试
- CI 管道：5 job CI + 多平台 release + 6 种安全扫描（pip-audit、npm audit、Trivy、CodeQL、Gitleaks、dependency review）
- 安全扫描每周定时执行（周一 06:00 UTC）
- Playwright E2E 测试已配置

### 部署

- Dockerfile 多阶段构建（Node 22 → Python 3.12-slim）+ 非 root 用户
- `docker-compose.yml` 已要求显式 `POSTGRES_PASSWORD`，并配置重启策略、资源限制与 `/readyz` healthcheck
- Alembic 迁移链完整（001 → 005），有 downgrade 路径，migration 003 做了全量幂等对账
- minikube 部署脚本可用，已通过真实 K8s Secret E2E 验证
- `/healthz` 作为 liveness，`/readyz` 作为 DB readiness，`/metrics` 暴露基础 Prometheus 文本指标

### 文档

- README 完整：架构概览、API 表格、时序图、部署指南、环境变量、安全设计
- 13 篇运维文档：部署、安全审计、预生产检查清单、告警、Agent 对比、发布模板
- OpenAPI / Swagger / ReDoc 自动生成，端点含双语 docstring
- 结构化 JSON 日志（`python-json-logger`），可配置日志级别
- 不可变审计日志（action / entity / actor / IP / timestamp）

---

## 三语言 Agent 功能对齐矩阵

| 功能 | Python | Go | Rust | 生产要求 |
|------|--------|-----|------|----------|
| TOFU 注册 | Yes | Yes | Yes | 必需 |
| 审批轮询 | Yes | Yes | Yes | 必需 |
| Heartbeat | Yes | Yes | Yes | 必需 |
| Fetch-certs | Yes | Yes | Yes | 必需 |
| 部署 + 回滚 | **Yes** | **Yes** | **No** | 必需 |
| Report-certs | No | **Yes** | No | 建议 |
| Nginx 自动检测 | No | **Yes** | No | 可选 |
| InsecureSkipVerify | No | **Yes** | No | 测试用 |
| RSA 密钥长度 | 3072 | 3072 | **2048** | 应统一 3072 |
| 单元测试 | 2 个（deployer） | **5 个** | 3 个（crypto） | 必需 |

**结论**：Python Agent 与 Go Agent 当前满足核心生产链路要求。Rust Agent 缺少回滚与 report-certs，建议仍作为实验性实现分阶段推进。

---

## 未覆盖测试的关键端点

### 后端

| 端点 | 位置 | 风险等级 |
|------|------|----------|
| `GET /api/control/agents/{id}/detail` | `control.py:194` | 中 |
| `GET /api/control/agents/{id}/assignments` | `control.py:595` | 中 |
| `DELETE /api/control/assignments/{id}` | `control.py:617` | 高 |
| `POST /api/control/rollouts` (API 层) | `control.py:685` | 中 |
| `POST /api/control/rollouts/{id}/rollback` (API 层) | `control.py:837` | 高 |
| `GET /api/control/dashboard/certs-expiry` | `dashboard.py:118` | 低 |
| `GET /api/control/dashboard/events-timeline` | `dashboard.py:201` | 低 |

### 客户端

| 模块 | Agent | 风险等级 |
|------|-------|----------|
| HTTP 客户端（全部方法） | Python | 高 |
| 密钥生成 + 指纹计算 | Python | 中 |
| 配置解析（from_env） | Python | 中 |
| 主循环 + 401/403 重注册 | Python | 高 |
| HTTP 客户端更多边界场景 | Go | 中 |

---

## 投产建议路线

| 阶段 | 内容 | 优先级 | 预估工作量 |
|------|------|--------|-----------|
| **P0 — 上线前必须** | 执行目标生产配置的预生产检查和最终发布 smoke | 高 | 0.5-1 天 |
| **P1 — 上线后 1 周内** | C2 Agent Token hash、业务级 Prometheus 指标、安全回归观察 | 高 | 1-2 天 |
| **P2 — 上线后 2 周内** | C8-C10（N+1 查询、复合索引、serial_hex 约束）、C15（覆盖率工具） | 中 | 2 天 |
| **P3 — 上线后 1 个月内** | C11-C14（Agent 功能对齐）、C16/C18-C21（审计归档、依赖锁、代码清理） | 低 | 5-7 天 |

---

## 与自测报告的交叉验证

| 自测报告声明 | 本次审查验证结果 |
|-------------|----------------|
| 后端 139 passed | 当前代码中 `def test_` 约 298 个函数定义（含参数化），pytest 实际执行用例数需以最新运行为准 |
| `dev-mode-bypass` 已移除 | 确认：前端代码中已无该字符串 |
| Compose 仅 db + app | 确认：`docker compose config --services` 输出 `db`, `app` |
| 截图文件完整 | 确认：23 张截图全部存在于 `specs/screenshots/` |
| smoke 脚本可用 | 确认：`tools/agent-smoke/run-live-smoke.sh` 及相关文件存在 |
| minikube 脚本可用 | 确认：4 个 `scripts/minikube-*.sh` 脚本存在 |
| K8s Secret E2E 通过 | 自测报告证据链完整（serial 匹配、RBAC 负例、浏览器截图） |
| 3 个失败用例已修复 | 确认：TC-E2E-010/011/012 对应的根因（8081 端口、nginx service、dev-mode-bypass）均已在代码中修复 |

---

## 签署

| 角色 | 签署 | 日期 |
|------|------|------|
| 评审人 | _________________ | __________ |
| 项目负责人 | _________________ | __________ |
| 运维负责人 | _________________ | __________ |
